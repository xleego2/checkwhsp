"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.EntityBuilder = exports.EntityType = void 0;
/** Every documented MessageEntity kind, typo-proof versus raw strings. */
exports.EntityType = {
    Mention: "mention",
    Hashtag: "hashtag",
    Cashtag: "cashtag",
    BotCommand: "bot_command",
    Url: "url",
    Email: "email",
    PhoneNumber: "phone_number",
    Bold: "bold",
    Italic: "italic",
    Underline: "underline",
    Strikethrough: "strikethrough",
    Spoiler: "spoiler",
    Blockquote: "blockquote",
    ExpandableBlockquote: "expandable_blockquote",
    Code: "code",
    Pre: "pre",
    TextLink: "text_link",
    TextMention: "text_mention",
    CustomEmoji: "custom_emoji",
};
/**
 * Accumulates styled text and the entities covering each styled segment, in
 * UTF-16 code units (JS string `.length`).
 */
class EntityBuilder {
    text = "";
    entities = [];
    /** Append text and (for a styled segment) an entity covering just that span. */
    append(segment, entity) {
        if (entity) {
            this.entities.push({
                ...entity,
                offset: this.text.length,
                length: segment.length,
            });
        }
        this.text += segment;
        return this;
    }
    /** Append unstyled text. */
    plain(s) {
        return this.append(s);
    }
    bold(s) {
        return this.append(s, { type: exports.EntityType.Bold });
    }
    italic(s) {
        return this.append(s, { type: exports.EntityType.Italic });
    }
    underline(s) {
        return this.append(s, { type: exports.EntityType.Underline });
    }
    strikethrough(s) {
        return this.append(s, { type: exports.EntityType.Strikethrough });
    }
    spoiler(s) {
        return this.append(s, { type: exports.EntityType.Spoiler });
    }
    code(s) {
        return this.append(s, { type: exports.EntityType.Code });
    }
    pre(s, language) {
        return this.append(s, { type: exports.EntityType.Pre, language });
    }
    /** A text_link entity pointing at `url`. */
    link(s, url) {
        return this.append(s, { type: exports.EntityType.TextLink, url });
    }
    /** A text_mention entity for a user without a username. */
    textMention(s, user) {
        return this.append(s, { type: exports.EntityType.TextMention, user });
    }
    customEmoji(s, customEmojiId) {
        return this.append(s, { type: exports.EntityType.CustomEmoji, custom_emoji_id: customEmojiId });
    }
    blockquote(s) {
        return this.append(s, { type: exports.EntityType.Blockquote });
    }
    /** The accumulated text plus the plain `MessageEntity[]` covering each segment. */
    build() {
        return { text: this.text, entities: this.entities };
    }
}
exports.EntityBuilder = EntityBuilder;
//# sourceMappingURL=entities.cjs.map